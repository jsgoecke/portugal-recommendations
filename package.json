{
  "name": "portugal-recommendations",
  "version": "1.0.0",
  "description": "Portugal with the Family — a single-page family travel guide.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./tailwind.css --minify && node stamp-css.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
